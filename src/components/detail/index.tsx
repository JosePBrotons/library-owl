import React from 'react';
import { Text, View } from 'react-native';

const Detail = () => {
    return (
        <View>
            <Text>{'Hola detail'}</Text>
        </View>
    );
};

export default Detail;
